<div class="columns is-mobile is-centered mt-3">
  <div class="column is-4-desktop is-12-tablet is-12-mobile">
    <% if @posts.present? %>
      <%= render partial: "photos/post", collection: @posts %>
    <% else %>
      <div class="top-margin-20">
        <p style="text-align: center"><%= t("photo_index.subscribe") %></p>
        <p style="text-align: center"><%= link_to t("photo_index.new_people"), home_url %></p>
      </div>
    <% end %>
  </div>
</div>